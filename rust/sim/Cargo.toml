# spectra-sim - concrete control simulator generated from the same guard AST.
# Spec: Part I 23, 24, 32.4.
# Status: not started.

[package]
name = "spectra-sim"
description = "SPECTRA concrete control replay simulator. Not started."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
# TODO(dependencies): none yet.

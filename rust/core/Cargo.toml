# spectra-core - canonical vocabulary types, identifiers, hashing.
# Spec: Part II 57.5/57.9 (rs = spectra_core::vocab::*), 58.
# Status: not started.

[package]
name = "spectra-core"
description = "SPECTRA canonical vocabulary and identifier types. Not started."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
# TODO(dependencies): none until the vocabulary types are written.

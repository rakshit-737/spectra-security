# spectra-liveness - per-source liveness and clock-skew passes.
# Spec: Part I 32.4; Part II 65 (liveness).
# Status: not started.

[package]
name = "spectra-liveness"
description = "SPECTRA per-source liveness and skew analysis. Not started."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
# TODO(dependencies): none yet.

# spectra_recon - state reconstruction, temporal and causal graph assembly.
# Spec: Part I 32.3, 13, 17, 19, 20.
# Status: not started.

[project]
name = "spectra-recon"
version = "0.0.0"
description = "SPECTRA state reconstruction and graph assembly. Not started."
requires-python = "==3.12.*"
license = { text = "Apache-2.0" }
dependencies = []
# TODO(dependencies): added in the commit that uses them (Part II 74.2).

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
# TODO(pin): see python/spectra_core/pyproject.toml.

[tool.hatch.build.targets.wheel]
packages = ["src/spectra_recon"]
